// wap to check given two numbewr are same or different.
#include <stdio.h>
void main()
{
    int a, b;
    printf("enter a : ");
    scanf("%d", &a);
    printf("enter b : ");
    scanf("%d", &b);
    a == b ? printf("num is same") : printf("number is differnt");
}